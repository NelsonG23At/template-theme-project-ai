import type { OverrideToken } from 'antd/es/theme/interface'
import { palette } from '../palette'

export const modalTokens: OverrideToken['Modal'] = {
  headerBg:      palette.neutral0,
  contentBg:     palette.neutral0,
  footerBg:      palette.neutral0,
  titleColor:    palette.neutral900,
  titleFontSize: 18,
}
