import type { OverrideToken } from 'antd/es/theme/interface'

export const spinTokens: OverrideToken['Spin'] = {
  contentHeight: 400,
  dotSize:       20,
  dotSizeSM:     14,
  dotSizeLG:     32,
}
