import type { OverrideToken } from 'antd/es/theme/interface'
import { palette } from '../palette'

export const tabsTokens: OverrideToken['Tabs'] = {
  inkBarColor:   palette.primary600,
  cardBg:        palette.neutral100,
  titleFontSize: 14,
  titleFontSizeLG: 16,
  cardHeight:    40,
}
